bundle agent deploy_test_hosts
{

  meta:
    "tags" slist => { "autorun" };
  vars:
    am_policy_hub::
      "names" slist => hostswithclass( "deploytesthost", "name" );
      "addresses" slist => hostswithclass( "deploytesthost", "address" );

  files:
    am_policy_hub::
      "/tmp/deploy_test_names.cfg"
        edit_line => insert_lines("name=$(names)"),
        create => "true";
      "/tmp/deploy_test_addresses.cfg"
        edit_line => insert_lines("address=$(addresses)"),
        create => "true";
}
